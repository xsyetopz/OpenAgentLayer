# Context7 Provider

Context7 is an optional CLI provider with `default = "cli-only"`.

- macOS/Linux install: `bunx --bun @upstash/context7`
- probe: `context7 --version`, `ctx7 --version`

`source/integrations/context7.toml` keeps it as third-party docs lookup with citation-backed output.
