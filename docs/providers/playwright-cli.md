# Playwright CLI Provider

Playwright CLI is an optional CLI provider with `default = "cli-only"`.

- macOS/Linux install: `bunx playwright-cli`
- probe: `playwright-cli --help`

It stays CLI-only in `source/providers.toml`.
