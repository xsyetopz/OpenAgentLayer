# Taste Skill Provider

Taste Skill is an exact-upstream provider with `default = "sync-only"`.

`source/providers.toml` records no shell install or probe commands.
Raw upstream and overlay paths stay under `providers/taste-skill/upstream` and `providers/taste-skill/overlay`.
