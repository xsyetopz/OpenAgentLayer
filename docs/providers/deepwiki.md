# DeepWiki Provider

DeepWiki is an optional CLI provider with `default = "cli-only"`.

- macOS/Linux install: `bunx deepwiki`
- probe: `deepwiki --help`

It stays CLI-only in `source/providers.toml`.
