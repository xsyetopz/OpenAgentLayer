# BMAD-METHOD Provider

BMAD-METHOD is an upstream-extraction provider with `default = "sync-extract"`.

`source/providers.toml` records no shell install or probe commands.
Raw upstream and overlay paths stay under `providers/bmad-method/upstream` and `providers/bmad-method/overlay`.
