# Caveman Integration

Caveman is a response-style integration.

OAL behavior:

- represent Caveman as a source-defined mode pack
- protect code, commands, paths, URLs, fenced code, and exact errors
- render native skill or hook context per adapter

Source spec: `source/integrations.toml`.
