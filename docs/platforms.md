# Platforms

Primary adapters:

- Codex: native `AGENTS.md`, config, hooks, skills, subagents, commands where supported.
- OpenCode: native config, agents, skills, permissions, MCP, local plugin only if source-proven.

Secondary adapters stay spec-only until primary adapters pass validation.

See `docs/platforms/codex.md` and `docs/platforms/opencode.md`.
