export function manifestInspect() {
	return {
		title: "Inspect OAL manifest ownership for the current project.",
		body: "Read the installed OAL manifest and return owned files, structured keys, marked blocks, and hash status.",
	};
}
