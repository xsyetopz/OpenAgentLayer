console.log(
	[
		"source/ is now the canonical framework source.",
		"bootstrap-source.mjs no longer regenerates source files from platform outputs.",
		"Edit source/ directly and run `bun run generate` instead.",
	].join("\n"),
);
