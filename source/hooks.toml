[[items]]
name = "pre-shell-guard"
kind = "pre"
route = "shell_guard"
purpose = "gate dangerous shell commands"
surfaces = ["codex", "claude", "opencode"]

[[items]]
name = "pre-rtk-enforce"
kind = "pre"
route = "rtk_enforce"
purpose = "rewrite or require RTK-backed shell commands"
surfaces = ["codex", "claude", "opencode"]

[[items]]
name = "pre-input-check"
kind = "pre"
route = "input_check"
purpose = "reject malformed tool input"
surfaces = ["claude"]

[[items]]
name = "post-shell-redact"
kind = "post"
route = "shell_redact"
purpose = "redact sensitive shell output"
surfaces = ["codex", "claude"]

[[items]]
name = "post-write-check"
kind = "post"
route = "write_check"
purpose = "check write/edit quality after file mutation"
surfaces = ["claude"]

[[items]]
name = "post-failure-circuit"
kind = "post"
route = "failure_circuit"
purpose = "track repeated tool failures"
surfaces = ["claude"]

[[items]]
name = "prompt-queue"
kind = "prompt"
route = "prompt_queue"
purpose = "inject queued prompt context"
surfaces = ["codex", "claude"]

[[items]]
name = "prompt-git-context"
kind = "prompt"
route = "git_context"
purpose = "inject compact repo state at prompt boundary"
surfaces = ["codex", "claude"]

[[items]]
name = "session-budget"
kind = "session"
route = "budget"
purpose = "initialize session budget context"
surfaces = ["codex", "claude"]

[[items]]
name = "session-cleanup"
kind = "session"
route = "cleanup"
purpose = "clear session-owned transient state"
surfaces = ["claude"]

[[items]]
name = "stop-scan"
kind = "stop"
route = "stop_scan"
purpose = "gate final response shape"
surfaces = ["codex", "claude"]

[[items]]
name = "subagent-route"
kind = "subagent"
route = "subagent_route"
purpose = "inject role route context at subagent start"
surfaces = ["claude"]

[[items]]
name = "subagent-scan"
kind = "subagent"
route = "subagent_scan"
purpose = "gate subagent completion output"
surfaces = ["claude"]
