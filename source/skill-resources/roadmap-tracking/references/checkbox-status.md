# Checkbox Status Convention

Use checkbox markers for tracker rows:

- `[ ]` not started
- `[~]` in progress
- `[x]` done

Apply this to `PLAN.md`, `ROADMAP.md`, and other checkbox-based Markdown tracking files.

Keep each row concrete and evidence-backed, then update the marker when repository evidence confirms status.
