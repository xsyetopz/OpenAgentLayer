[platform_spec]
required_sections = [
    "references",
    "native_surfaces",
    "adapter_plan",
    "generated_paths",
    "install_paths",
    "validation",
]

[public_surface]
allowed_names = ["OpenAgentLayer", "OAL", "oal", "oal-runner"]

[checks]
models = "supported model IDs only"
runner = "command policy, RTK probing, and token accounting fields"
platforms = "native surfaces, generated paths, install paths, and validation fixtures"
