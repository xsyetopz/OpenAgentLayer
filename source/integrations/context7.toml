[integration]
name = "context7"
provider = "context7"
kind = "third-party-docs-lookup"
ownership = "external"
vendored = false

[doctor]
commands = ["context7 --version", "ctx7 --version"]
required = false

[policy]
use_when = "third-party library, API, setup, or config docs are needed"
output = "citation-backed research"
fallback = "native platform MCP or explicit blocker when unavailable"
