## Codex baseline

- **Baseline source:** use Codex bundled base instructions plus OAL project guidance from this file.
- **Source order:** follow OAL source records, generated artifact contracts, manifest ownership, and route evidence before general assistant defaults.
- **Generated files:** generated files are disposable outputs; update `source/`, renderer code, plugin payloads, or deploy logic, then regenerate or validate.
- **Provider surface:** use provider-native Codex surfaces that OAL renders and acceptance verifies.
- **Instruction reload surface:** treat `AGENTS.md` as session-loaded project guidance. Do not assume edits to it are visible inside an already-running thread unless current Codex source or a fresh resume proves reload. Put fast-changing workflows in skills, hooks, commands, or rendered agents; Codex watches local skill roots, clears skill cache on changes, and reads invoked skill bodies from disk for the turn.
- **Subagent surface:** OAL defaults Codex orchestration to OpenDex/Symphony with native `multi_agent_v2`, `multi_agent`, and fanout disabled. Default Codex agents should not ask native Codex agents to spawn; use `oal codex peer batch <task>`, `oal opendex`, `opendex`, or Symphony workflow commands for bounded peer orchestration. Operators can opt into stable `multi_agent` or `multi_agent_v2` through OAL CLI setup options. Stable `multi_agent` is bounded by rendered `agents.max_threads` and `agents.max_depth`; `multi_agent_v2` uses its own `max_concurrent_threads_per_session` throttle and omits disallowed `agents.max_threads`. When native Codex agents are explicitly selected, spawn rendered OAL agents by name and merge only final summaries, diff evidence, and validation results in the parent thread. The parent thread owns task split, child launch, evidence merge, and final decision.
- **Module shape:** do not build god objects, catch-all managers, or one-file products when behavior has separate reasons to change. Before broad implementation, name the owning modules or seams, keep orchestration, persistence, provider adapters, runtime accounting, prompts, and UI/config surfaces separate, and expose a small facade only when callers need one stable entry point.
- **Final response:** include concrete validation evidence or a precise blocker.
