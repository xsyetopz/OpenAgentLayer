# Implement

Implement `$ARGUMENTS` as production OAL work using accepted source/spec constraints.

## Route

- Owner role: `hephaestus`.
- Required skill context: openagentsbtw, review.
- Input argument: `$ARGUMENTS`.

## Procedure

- Lock the requested objective and current dirty-tree ownership before editing.
- Change source records, package code, runtime scripts, docs, and tests together when behavior crosses those boundaries.
- Preserve provider-native Codex, Claude, and OpenCode output shapes.
- Return changed paths, validation commands, and blockers only when concrete.

## Acceptance

- Implementation, docs/specs, and validation agree; no placeholder route remains.
- Output includes evidence, validation, changed paths when edits happen, and blockers when blocked.
