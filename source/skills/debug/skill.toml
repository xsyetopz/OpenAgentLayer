id = "debug"
kind = "skill"
title = "Debug"
description = "Structured debugging workflow for reproducing failures, narrowing root causes, and collecting evidence before changing code. Triggers: debug, debugging, root cause, reproduce, failing behavior, broken flow, investigate bug, triage failure, isolate issue, regression analysis."
body = "SKILL.md"
license = "MIT"
compatibility = "Codex, Claude, and OpenCode Agent Skills surfaces."
triggers = ["debug", "reproduce", "root cause"]
when_to_use = "Structured debugging workflow for reproducing failures, narrowing root causes, and collecting evidence before changing code. Triggers: debug, debugging, root cause, reproduce, failing behavior, broken flow, investigate bug, triage failure, isolate issue, regression analysis."
invocation_mode = "manual-or-route"
user_invocable = true
allowed_tools = ["read", "search"]
tool_grants = ["read", "search"]
route_contract = "readonly"
references = []
scripts = []
assets = []
supporting_files = []
surfaces = ["codex", "claude", "opencode"]

[metadata]
origin = "openagentlayer-local"
source_package = "openagentlayer"
skill_category = "oal-core"
upstream_name = "debug"
