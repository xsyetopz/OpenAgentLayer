id = "git-workflow"
kind = "skill"
title = "Git Workflow"
description = "Git workflow conventions for commits, branches, and PRs. Use when the user mentions commit flow, PR flow, branching, rebasing, release hygiene, or Git workflow rules."
body = "SKILL.md"
license = "MIT"
compatibility = "Codex, Claude, and OpenCode Agent Skills surfaces."
triggers = ["git-workflow"]
when_to_use = "Git workflow conventions for commits, branches, and PRs. Use when the user mentions commit flow, PR flow, branching, rebasing, release hygiene, or Git workflow rules."
invocation_mode = "manual-or-route"
user_invocable = true
allowed_tools = ["read", "search"]
tool_grants = ["read", "search"]
route_contract = "readonly"
references = []
scripts = []
assets = []
supporting_files = []
surfaces = ["codex", "claude", "opencode"]

[metadata]
origin = "openagentlayer-local"
source_package = "openagentlayer"
skill_category = "oal-core"
upstream_name = "git-workflow"
