# Using this research pack with Codex

Do not ask Codex to summarize these docs into more docs.

Use this pack as implementation evidence. The worker should build provider-native render/deploy behavior and then cite these files only to justify the implementation.

Operating instruction:

```text
Use the provider research pack as implementation evidence. Do not create additional research docs. Build code that renders and deploys the supported Codex, Claude Code, and OpenCode surfaces described here. If a generated artifact is not consumed by render/deploy/validation/runtime behavior, delete it.
```

Acceptance remains the source of truth: generation, deploy, uninstall, hooks, tools, commands, config parseability, model allowlists, manifest ownership.
